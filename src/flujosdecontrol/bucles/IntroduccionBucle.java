package flujosdecontrol.bucles;

/** BUCLES
 * SENTENCIA while
 * Ejecuta un bloque de codigo mientras cumpla la condicion, ysi no no dentra en el bucle
 *
    * while(condicion) {
     *     --- instrucciones ---
     * }
 * SENTENCIA do while
 * Similar al while, excepto que la expresion se evalua al final del bucle, por lo
 * cual se ejecutara minimo una sola vez.
     * do{
     *    --- instrucciones ---
     * } while(condicion);
 *
 * Sentencia for
 * La sentencia for se utiliza cundo se conocen los limites del bucle.
 * for(inicializacion; condicion; incremento/decremento) {
 *  --- instrucciones ---
 * }
 */

public class IntroduccionBucle {
    public static void Operaciones() {
        // BucleFor.Operacion();
        // BucleDoWhile_While.Operaciones();
        // BucleForEach.Operaciones();
        // EtiquetasBucles.Operaciones();
        EjemplosBucles.Ejemplos();
    }


}
